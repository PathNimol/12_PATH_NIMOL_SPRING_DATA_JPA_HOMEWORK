services:
  # Database
  postgres:
    image: postgres
    container_name: hibernate-jpa-01-db
    volumes:
      - hibernate-jpa-01-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: 151003
      POSTGRES_USER: postgres
      POSTGRES_DB: hibernate_jpa_01_db
    healthcheck:
      test: [ "CMD", "pg_isready", "-U", "keycloak" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - network

  # API
  springboot-api:
    build:
      context: .
    container_name: hibernate-jpa-01-api
    ports:
      - "8081:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/hibernate_jpa_01_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: 151003
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - network

volumes:
  hibernate-jpa-01-data:

networks:
  network:
    name: network
